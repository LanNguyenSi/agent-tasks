{
  "name": "@agent-tasks/cli",
  "version": "0.3.0",
  "description": "CLI client for agent-tasks, task management for local AI agents",
  "license": "MIT",
  "type": "module",
  "bin": {
    "agent-tasks": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "pretest": "npm run build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "typecheck:test": "tsc --noEmit -p tsconfig.test.json",
    "prepublishOnly": "npm run build"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/LanNguyenSi/agent-tasks.git",
    "directory": "cli"
  },
  "homepage": "https://github.com/LanNguyenSi/agent-tasks/tree/master/cli#readme",
  "bugs": {
    "url": "https://github.com/LanNguyenSi/agent-tasks/issues"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.22.4",
    "typescript": "^5.7.0",
    "vitest": "^4.1.8"
  },
  "overrides": {
    "postcss": "^8.5.18"
  },
  "//overrides": "Inert for the monorepo workspace install (npm reads `overrides` only from the root manifest of an install) and for consumers of the published package; kept in case cli/ is ever cloned and installed as a standalone project again, as it originally was before joining this workspace."
}
