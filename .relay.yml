name: agent-tasks
health: /api/health
compose_file: docker-compose.prod.yml
pre_update:
  - docker compose -f docker-compose.prod.yml build --no-cache
post_update:
  - docker exec agent-tasks-backend-1 npx prisma db push --skip-generate
rollback: true
